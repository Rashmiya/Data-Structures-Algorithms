package linkedList;

public class doublyNode {
    int val;
    doublyNode next;
    doublyNode previous;

    // create the constructor
    public doublyNode(int val,doublyNode next, doublyNode previous){
        this.val = val;
        this.next = next;
        this.previous = previous;
    }
}
